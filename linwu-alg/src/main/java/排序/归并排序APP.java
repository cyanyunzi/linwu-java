package 排序;

/**
 * 1. 大数组拆分左右子数组
 * 2. 左右子数组对比值,放入汇总数组返回
 */
public class 归并排序APP {
    public static void main(String[] args) {
        int[] arr = {0, 4, 2, 7, 18, 15, 3};
//        System.out.println(Arrays.toString(sort(arr)));
    }

    public static void sort(int[] arr) {
        int[] temp = new int[arr.length];
        int mid = temp.length / 2;
    }

    public static void sort(int[] temp, int left, int rigt) {
        if (left < rigt) {

        }
    }

    public static void merge(int[] temp, int left, int mid) {

    }
}